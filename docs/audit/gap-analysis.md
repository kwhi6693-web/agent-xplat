# Agent Xplat v1.0 Release-Preparation Gap Analysis

Audit runs: initial `VX-2026-09-01-002`; post-hardening review `VX-2026-09-01-003`; hosted release verification `VX-2026-09-01-004`
Specification: the original Agent Xplat v1.0 implementation prompt plus `docs/task-specification.md` v1.0
Audit scope: current implementation after the JavaScript/TypeScript AST hardening pass, public GitHub push, hosted runner execution, artifact readback, and final local release checks
Current evidence: local `python -m pytest -q` -> `88 passed`; fresh isolated editable and wheel installs passed; hosted run `33511871082` passed on all three required runners; `git diff --check` -> pass

This is an implementation audit, not a release approval. The rows below preserve the original gap discovery and record the post-hardening disposition. A row marked `PARTIAL` or `OPEN` is an actionable limitation even when the broader capability is implemented.

| Requirement | Implementation | Test | Evidence | Gap | Action |
|---|---|---|---|---|---|
| Architecture and staged Gates | `docs/ARCHITECTURE.md`, project task specification, staged audit records, modular `src/agent_xplat/` package | Existing unit/integration suite, focused AST plan, and this post-hardening gate record | Local baseline is reproducible; no hosted run is available | Hosted execution remains an evidence gate, not an architecture gap | Preserve staged local gates and close the hosted evidence gate before release |
| OS × Shell × Runtime model | Eight explicit targets in `environments.py`; target IDs flow into findings and scores; WSL is treated as Linux for Node `process.platform` | `tests/test_models_config_discovery.py`, report assertions, Node runtime mapping tests | Matrix is emitted with distinct Windows shell/runtime rows | Model remains a fixed first-party profile set and does not claim arbitrary shell-version capability discovery | Preserve the model and expose parser/runtime evidence as metadata without collapsing dimensions |
| Required file discovery | `discovery.py` covers agent files, `.github/**`, `.cursor/**`, `.claude/**`, `.codex/**`, `scripts/**`, package/Python/container/make files and `.js/.mjs/.cjs/.jsx/.ts/.mts/.cts/.tsx` | Discovery bounds/exclude tests and dialect-extension test | Deterministic traversal, binary detection, size bound, default excludes, and all supported source suffixes are tested | Generated directories and files above the configured bound remain intentionally skipped | Keep the documented bounds and add new suffixes only with parser coverage |
| Rule modularity and metadata | 49 registered `RuleSpec` entries with severity, confidence, remediation, examples, test-case text, and detector | Registry/documentation, rule, fixture, negative-control, and snapshot tests | `docs/RULES.md` mirrors the registry; AST rules have dedicated fixture assertions | Not every lexical rule has a one-to-one positive/negative fixture pair, although the material release paths are covered | Add narrowly scoped fixture pairs with future rule changes; do not expand the core scope for ceremony |
| Path portability | `rules/paths.py` detects selected POSIX/Windows paths, variables, separators, launchers, and reserved names; metadata checks handle collision, Unicode, and length; Node path APIs are binding-aware | Path, metadata, fixture, and false-positive tests | Paths with spaces and `$USERPROFILE` are located; bound `path.join`/`path.resolve` literals are not reported as separator issues | General Markdown/shell text still uses bounded lexical heuristics, and dynamic path construction is not provable statically | Keep lexical findings conservative and retain AST suppression only for structurally recognized path APIs |
| Shell command portability | `rules/shell.py` has target-aware lexical checks for POSIX utilities, `chmod`, env assignment, `source`, `where`, `set`, chaining, and semicolons; bound Node child-process strings use AST | Shell/path rule tests, shell fixtures, Node AST tests, and negative controls | Native Windows vs Git Bash/WSL targeting is observable; unrelated JS member names are not treated as shell commands | Text files remain intentionally heuristic and may need runtime confirmation for ambiguous prose | Keep text analysis bounded and reserve high-confidence embedded-command findings for AST-bound calls |
| Quoting and command syntax | `rules/quoting.py` detects interpolation, substitution, continuations, redirection, caret escaping, and pipelines | Additional rule tests and quoted/AST negative controls | Target-specific findings are emitted; JavaScript strings are excluded from lexical shell rules | No shell parser; quote state is line-local and ambiguous prose remains heuristic | Keep ambiguous lexical findings at the documented severity/confidence and require runtime confirmation where behavior matters |
| Python structured analysis | Python `ast` parser and AST-backed import/interpreter checks in `parsers.py` and `rules/python.py` | Parser and Python rule tests | Python syntax errors do not execute code; AST findings have locations | Version requirements, platform branches, and native dependency detection remain partly lexical and not fully guard-aware | Retain Python AST path; add only directly relevant regression coverage while avoiding scope expansion |
| JavaScript/TypeScript structured analysis | `parsers.py` selects Tree-sitter JavaScript, TypeScript, or TSX by suffix; `rules/node_ast.py` performs binding-aware child-process, platform, environment, executable, and command analysis | Parser tests, all-suffix routing test, Node AST fixture, guarded/unguarded and malformed-source tests | `.js/.mjs/.cjs/.jsx/.ts/.mts/.cts/.tsx` are parsed without importing or executing source; recovered syntax downgrades high-confidence AST findings | Dynamic/generated code, unsupported grammar recovery, and runtime behavior remain static-analysis limits | Keep AST findings `INFERRED`, preserve conservative confidence, and require runtime verification for execution claims |
| Node package-script analysis | `parse_json()` validates manifests; a JSON token walk scopes script regexes to decoded `scripts` values and dependency checks to dependency-object keys | `tests/test_rules_languages.py`, parser span tests, Node fixture | Descriptions and dependency names no longer create package-script false positives; exact source columns are preserved | Script contents still use bounded lexical classification rather than a shell parser | Keep scoped lexical analysis and document shell-version ambiguity |
| Filesystem portability | CRLF shebang, links, locking names, rename assumptions, collisions, long/illegal/Unicode names are covered by separate detectors | Additional rules and metadata tests | Repository metadata findings use actual paths | Executable-bit metadata and several atomic/locking semantics are only inferred; no real filesystem matrix exists locally | Keep findings conservative and disclose runtime limits; add fixture coverage for material syntax cases |
| External tools and package managers | Lexical `external_tools.py` and `package_managers.py`, with recognition of explicit GitHub setup actions | Language/additional rules, fixture, and workflow false-positive tests | OS-specific manager impact is target-specific; generated `setup-python` CI is not reported as an unverified PATH dependency | Tool names in prose remain difficult to distinguish from executable requirements; install instructions are not fully structurally modeled | Keep contextual negative tests and leave unprovable availability as WARNING/MEDIUM |
| Static safety boundary | Scan/report/fix/baseline/diff/init/doctor avoid target execution; `test` is the explicit boundary; Tree-sitter receives bytes only | `tests/test_cli_extra.py`, parser no-execution test, verification tests | Scan of dangerous Python/JS source does not create its marker; parser dependencies are declared package inputs, not target-code execution | Installation may access package indexes when the user installs the tool; scan itself does not network | Retain no-execution tests and keep dependency/install behavior documented |
| Severity and confidence | Enums and per-rule metadata; findings preserve both; recovered AST syntax downgrades HIGH to MEDIUM | Rule, fixture, negative-control, and malformed-source assertions | Direct bound calls are HIGH; platform/env inference is MEDIUM; semicolon/ambiguous rules remain LOW | Broad lexical text rules can still represent documented assumptions rather than executable code | Keep HIGH limited to direct evidence and do not auto-fix AST findings |
| Deterministic scoring | Fixed severity penalties, target-specific aggregation, stable ordering/fingerprints | `tests/test_engine_scoring.py` and repeated JSON checks | Score math is reproducible locally | Duplicate related signals can compound penalties; report does not expose penalty math per finding | Preserve deterministic formula, add AST score assertions, and document duplicate-signal behavior |
| Compatibility Contract | `contracts.py` compares declared supported targets with blockers/errors and minimum score | Contract/suppression tests | Supported target with blocker produces `VIOLATION` | `requirements` are echoed but not evaluated against detected/runtime facts; unsupported declarations are informational only | Keep static contract semantics explicit; add contract tests for AST assumptions and disclose runtime/version limits |
| Configuration schema | Documented YAML subset, wrapper form, targets/excludes/ignore/threshold/fail-on/verification validation; duplicate keys and unknown ignore IDs rejected | Configuration tests | Invalid target, duplicate key, unknown rule, type, and verification input cases are covered | It is not a general YAML parser; unsupported YAML features are intentionally rejected | Keep the narrow schema and document unsupported YAML rather than adding a full YAML dependency |
| Suppression and auditability | Global and line-level marker suppression keeps findings and counts; unused line markers become summary/report diagnostics | `tests/test_suppression_contract.py`, JSON/Markdown output checks | Ignored findings remain machine-readable and unused markers are not silently accepted | Diagnostics do not create a portability gate failure by themselves | Preserve explicit diagnostics and keep suppression behavior auditable |
| Baseline | JSON fingerprints and existing/new/resolved sets | Baseline tests and CLI regression | New finding gate works locally | Baseline is fingerprint/location-sensitive and does not retain finding detail for machine consumers | Preserve v1.0 keys; add detail only compatibly if needed and test movement behavior |
| Git Diff Mode | Safe `git archive` reference materialization and score/fingerprint comparison | Git diff tests | Reference files are not executed; before/after scores are rendered | Diff report currently exposes counts/scores but not full new/resolved finding detail; Git availability is required | Add detail only if it remains stable and keep invalid-reference exit code 2 |
| Explain | `explain.py` renders registered rule meaning, rationale, examples, remediation | CLI explain test and registry/documentation coverage | Known rules, including AST rules, explain; unknown rule returns 2 | Explain output is text rather than a separately versioned machine schema | Preserve stable rule IDs and catalog metadata; keep the human-readable contract documented |
| Fix Engine | Only CRLF shebang normalization is auto-fixable; hashes/dry-run/idempotence are implemented | `tests/test_fixing.py` | No high-risk rewrite is attempted | AST findings must not gain autofix without an equivalence proof and tests | Keep AST rules suggestion-only in v1.0 |
| Runtime verification | Allowlisted no-shell-operator command with timeout; result labels the actual host | `tests/test_verification.py`, local `agent-xplat test .`, and GitHub Actions run `33511871082` | Current Windows host and all three hosted OS jobs are independently recorded as `VERIFIED`; static results remain `INFERRED` | The runtime command proves only the recorded host and project test command, not every shell row in the eight-target matrix | Preserve `INFERRED`/`VERIFIED` distinction and require separate evidence for additional runtime classes |
| GitHub Actions | Three-runner workflow installs, tests, scans, runs controlled verification, uploads JSON/SARIF/Markdown | Workflow content test, job/step readback, artifact API readback, and validators | `windows-latest`, `macos-latest`, `ubuntu-latest` all passed in run `33511871082` and produced non-empty artifacts bound to its head SHA | GitHub action deprecation annotations are non-failing maintenance warnings; they do not invalidate the recorded run | Preserve the current workflow and schedule action-version maintenance separately from this frozen v1.0 scope |
| JSON result contract | Versioned result object with targets, scores, findings, baseline, contract, verification, summary; nested validator and `RESULT_SCHEMA` shape | JSON validator, AST metadata, malformed nested-shape tests | Generated documents validate; positive coordinates/enums/nested types are checked | Baseline/contract/verification objects retain extensible command-specific keys rather than a closed universal schema | Keep additive metadata forward-compatible and gate on `schema_version` |
| SARIF | SARIF 2.1.0 output with rules, locations, levels, help, fingerprints | SARIF validator and AST SARIF location/help test | Generated Node AST SARIF validates locally with source coordinates | Project validator is intentionally a focused shape validator, not the full external SARIF schema | Keep output standard-shaped and use GitHub Code Scanning as hosted integration evidence |
| Markdown/terminal UI | Matrix, issues, assumptions, contract, fixes, verification, ignored findings, baseline/diff sections | Report tests and `tests/test_snapshots.py` | Non-color terminal output is readable; mixed fixture output is locked by a snapshot; required Markdown sections are present | Snapshot intentionally covers a representative stable fixture, not every report variant | Refresh snapshots only with an intentional output-contract change |
| Badge and doctor | Badge distinguishes static/runtime; doctor probes local capabilities only | Init/badge/doctor tests | Runtime badge refuses incomplete three-OS evidence | Badge evidence validation is minimal and doctor does not test versions/capabilities beyond command presence | Preserve truthful labels; avoid expanding doctor into repo health |
| Agent-native CLI and exit codes | Ten required commands, JSON output, codes 0/1/2/3 | CLI/help/error and regression tests | All command help probes pass locally | Some command-specific invalid-input and expected-failure cases are not individually covered; argparse behavior is only indirectly tested | Add focused CLI cases for new parser/config failures and stable JSON behavior |
| Fixtures and negative coverage | Ten broad fixture directories remain, plus dedicated `tests/fixtures/node-ast` cases for JavaScript/TypeScript AST findings and guarded code | Fixture catalog, exact-rule assertions, parser/rule tests, and negative controls | Node AST fixture has expected rules, affected targets, severity, confidence, and a no-finding guarded example | Not every registered lexical rule has a one-to-one fixture pair; that is a coverage-depth gap, not an AST release blocker | Keep broad fixture coverage and add pairs only when a rule change materially affects release behavior |
| Packaging and stack decision | Python 3.10+, setuptools, standard-library-first core with narrowly scoped Tree-sitter JavaScript/TypeScript bindings, MIT metadata | Wheel metadata, editable isolated install, wheel install, and parser import/scan evidence | Declared parser dependencies install in the isolated Windows environment and the wheel contains the package entry point | Other OS package installation and wheel compatibility still need hosted runner evidence | Keep the narrow dependency surface; verify the published artifact on all three hosted OS runners |
| Documentation truthfulness | README, architecture, rules, JSON schema, ADRs, and project facts now describe Tree-sitter AST coverage and the static/runtime boundary | Documentation contract tests, manual readback, command probes, and this post-hardening audit | Public docs no longer call JavaScript/TypeScript AST a future limitation; hosted verification remains explicitly unclaimed | Historical audit records still need their post-hardening evidence pointers refreshed | Update audit records from the final local run and retain historical gaps as dated evidence |
| Release metadata and repository audit | LICENSE, security, contribution, issue/PR/release templates, package metadata, audit docs, workflow, public remote, and hosted artifacts are present | Secret/path/package/workflow audit, public remote readback, wheel inspection, artifact validators, and README demo readback | No secrets, protected-path references, debug artifacts, or telemetry were found; parser dependencies are declared; remote `master` and public workflow/README blobs match the pushed commit; hosted artifacts are non-empty and valid | GitHub reports non-failing action maintenance annotations; no v1.0 release-blocking repository gap remains | Record the public readback and preserve action maintenance as separate follow-up work |
| Full v1.0 release readiness | Core implementation, local release chain, public repository, and hosted evidence are complete | Final local regression, isolated installs, CLI chain, artifact validators, hosted job/step readback, and repository audit | AC-12 is PASS from run `33511871082`; all required runner/artifact evidence is current for the verified source commit | No release blocker identified within the frozen v1.0 scope | Complete the final gate record and retain the run/artifact evidence |

## Priority order

1. ~~Close the JavaScript/TypeScript AST gap and add guarded/unguarded source fixtures.~~ Completed in the post-hardening pass.
2. ~~Restrict Node package-script rules to parsed `scripts` values to reduce false positives.~~ Completed and covered by scoped-span tests.
3. ~~Expand discovery and parser tests for `.jsx`, `.mts`, `.cts`, and `.tsx`.~~ Completed for the documented source suffixes.
4. ~~Reconcile rule docs, ADRs, README limitations, JSON/SARIF contracts, and audit counts.~~ Completed for implementation-facing docs; final evidence records remain to be refreshed.
5. ~~Re-run the complete local regression and package installation checks.~~ Completed locally; final post-edit rerun is part of release evidence.
6. ~~Obtain real `windows-latest`, `macos-latest`, and `ubuntu-latest` runner evidence before the final release decision.~~ Completed in hosted run `33511871082`; final gate record follows.

## Explicit non-goals for this pass

- No SaaS, GitHub App, MCP server, editor extension, telemetry, AI API, or repository-health expansion.
- No broad shell parser or high-risk automatic rewrite without a deterministic proof and test suite.
- No claim that static AST analysis is runtime verification.
