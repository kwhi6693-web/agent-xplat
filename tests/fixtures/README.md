# Fixture catalog

Each fixture is intentionally small and contains `expected.json` with the rule IDs, affected target examples, severity, and confidence expected from the analyzer. Fixtures are source examples, not claims that a third-party agent tool follows a common standard.
